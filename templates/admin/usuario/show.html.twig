{% extends 'base.html.twig' %}

{% block title %}Usuario{% endblock %}

{% block actions %}
{% endblock %}

{% block body %}
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ usuario.id }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ usuario.email }}</td>
            </tr>
            <tr>
                <th>Roles</th>
                <td>{{ usuario.roles ? usuario.roles|json_encode : '' }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ usuario.password }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ usuario.nombre }}</td>
            </tr>
            <tr>
                <th>Perfil</th>
                <td>{{ usuario.perfil }}</td>
            </tr>
        </tbody>
    </table>
    <div class="flex">
    <button class="btn btn-info" href="{{ path('app_admin_usuario_edit', {'id': usuario.id}) }}">edit</button>
    {{ include('admin/usuario/_delete_form.html.twig') }}
    </div>
{% endblock %}
